services:
  alexandria-backend:
    extends:
      file: ./env-compose.yml
      service: alexandria-app-base
    container_name: alexandria-backend
    command: >
      sh -c "uv run --locked --no-editable alembic upgrade head && exec /app/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/ready"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    ports:
      - "127.0.0.1:8000:8000"
    depends_on:
      alexandria-postgres:
        condition: service_healthy
      alexandria-redis:
        condition: service_healthy

  # Reindexing is CPU-heavy and retryable. A dedicated process keeps bounded
  # embedding work and Redis Streams retries outside HTTP/MCP request handling.
  alexandria-maintenance-worker:
    extends:
      file: ./env-compose.yml
      service: alexandria-app-base
    container_name: alexandria-maintenance-worker
    command:
      - uv
      - run
      - --locked
      - --no-editable
      - python
      - -m
      - app.operations.workers.redis_maintenance_worker
    depends_on:
      alexandria-backend:
        condition: service_healthy
      alexandria-redis:
        condition: service_healthy

  alexandria-postgres:
    container_name: alexandria-postgres
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: "${ALEXANDRIA_POSTGRES_DB}"
      POSTGRES_USER: "${ALEXANDRIA_POSTGRES_USER}"
      POSTGRES_PASSWORD: "${ALEXANDRIA_POSTGRES_PASSWORD}"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\"",
        ]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      alexandria-network:
        aliases:
          - postgres

  alexandria-redis:
    container_name: alexandria-redis
    image: redis:8.8.1-alpine
    env_file:
      - ./runtime-performance.env
    entrypoint: ["/bin/sh", "/usr/local/bin/alexandria-redis-start.sh"]
    volumes:
      - ./scripts/redis-start.sh:/usr/local/bin/alexandria-redis-start.sh:ro
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    restart: unless-stopped
    networks:
      - alexandria-network

  alexandria-graph:
    container_name: alexandria-graph
    image: neo4j:5-community
    env_file:
      - ./runtime-performance.env
    environment:
      ALEXANDRIA_NEO4J_PASSWORD: "${ALEXANDRIA_NEO4J_PASSWORD}"
    entrypoint: ["/bin/sh", "/usr/local/bin/alexandria-neo4j-start.sh"]
    ports:
      - "127.0.0.1:7474:7474"
      - "127.0.0.1:7687:7687"
    volumes:
      - ./scripts/neo4j-start.sh:/usr/local/bin/alexandria-neo4j-start.sh:ro
      - neo4j-data:/data
      - neo4j-logs:/logs
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "cypher-shell --non-interactive --format plain -u neo4j -p \"$${ALEXANDRIA_NEO4J_PASSWORD:?}\" 'RETURN 1;' >/dev/null",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    networks:
      alexandria-network:
        aliases:
          - neo4j

volumes:
  embedding-model-cache:
    name: alexandria-hermes_embedding-model-cache-v2
  postgres-data:
  redis-data:
  neo4j-data:
  neo4j-logs:

networks:
  alexandria-network:
    driver: bridge
